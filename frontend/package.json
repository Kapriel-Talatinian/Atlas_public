{
  "name": "atlas-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api": "dotnet run --project ../src/Atlas.Api --urls http://127.0.0.1:5000",
    "dev": "vite",
    "dev:full": "concurrently -k -n api,web -c green,cyan \"npm run api\" \"npm run dev\"",
    "build": "vite build",
    "serve": "vite preview",
    "serve:prod": "serve -s dist -l ${PORT:-4173}"
  },
  "dependencies": {
    "@blueprintjs/core": "^6.12.0",
    "@blueprintjs/icons": "^6.9.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^2.6.2",
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "concurrently": "^9.2.1",
    "vite": "^4.0.0"
  }
}
